--- a/CMakeLists.txt
+++ src/ros-controls/realtime_tools/CMakeLists.txt
@@ -48,7 +48,7 @@ target_include_directories(realtime_tools PUBLIC
   $<INSTALL_INTERFACE:include/realtime_tools>
 )
 ament_target_dependencies(realtime_tools PUBLIC ${THIS_PACKAGE_INCLUDE_DEPENDS})
-if(UNIX)
+if(UNIX AND NOT APPLE)
   target_link_libraries(realtime_tools PUBLIC cap)
 endif()
 
@@ -62,7 +62,7 @@ target_include_directories(thread_priority PUBLIC
   $<INSTALL_INTERFACE:include/realtime_tools>
 )
 ament_target_dependencies(thread_priority PUBLIC ${THIS_PACKAGE_INCLUDE_DEPENDS})
-if(UNIX)
+if(UNIX AND NOT APPLE)
   target_link_libraries(thread_priority PUBLIC cap)
 endif()
 
--- a/src/realtime_helpers.cpp
+++ src/ros-controls/realtime_tools/src/realtime_helpers.cpp
@@ -32,7 +32,9 @@
 #include <windows.h>
 #else
 #include <sched.h>
+#if defined(__linux__)
 #include <sys/capability.h>
+#endif
 #include <sys/mman.h>
 #include <sys/utsname.h>
 
@@ -77,6 +79,9 @@ bool configure_sched_fifo(int priority)
 #ifdef _WIN32
   HANDLE thread = GetCurrentThread();
   return SetThreadPriority(thread, priority);
+#elif defined(__APPLE__)
+  (void)priority;
+  return false;
 #else
   struct sched_param schedp;
   memset(&schedp, 0, sizeof(schedp));
@@ -95,24 +97,25 @@ std::pair<bool, std::string> lock_memory()
 #ifdef _WIN32
   return {false, "Memory locking is not supported on Windows."};
 #else
-  auto is_capable = [](cap_value_t v) -> bool {
-    bool rc = false;
-    cap_t caps;
-    if ((caps = cap_get_proc()) == NULL) {
-      return false;
-    }
-
-    if (cap_set_flag(caps, CAP_EFFECTIVE, 1, &v, CAP_SET) == -1) {
-      rc = false;
-    } else {
-      rc = (cap_set_proc(caps) == 0);
-    }
-    cap_free(caps);
-    return rc;
-  };
-
   std::string message;
   if (mlockall(MCL_CURRENT | MCL_FUTURE) == -1) {
+#if defined(__linux__)
+    auto is_capable = [](cap_value_t v) -> bool {
+      bool rc = false;
+      cap_t caps;
+      if ((caps = cap_get_proc()) == NULL) {
+        return false;
+      }
+
+      if (cap_set_flag(caps, CAP_EFFECTIVE, 1, &v, CAP_SET) == -1) {
+        rc = false;
+      } else {
+        rc = (cap_set_proc(caps) == 0);
+      }
+      cap_free(caps);
+      return rc;
+    };
+
     if (!is_capable(CAP_IPC_LOCK)) {
       message = "No proper privileges to lock the memory!";
     } else if (errno == ENOMEM) {
@@ -131,6 +134,9 @@ std::pair<bool, std::string> lock_memory()
     } else {
       message = "Unknown error occurred!";
     }
+#else
+    message = "Unable to lock memory: " + std::string(std::strerror(errno));
+#endif
     return {false, message};
   } else {
     message = "Memory locked successfully!";
@@ -146,6 +152,9 @@ std::pair<bool, std::string> set_thread_affinity(
 #ifdef _WIN32
   message = "Thread affinity is not supported on Windows.";
   return std::make_pair(false, message);
+#elif defined(__APPLE__)
+  message = "Thread affinity is not supported on macOS.";
+  return std::make_pair(false, message);
 #else
   auto set_affinity_result_message = [](int result, std::string & msg) -> bool {
     if (result == 0) {
