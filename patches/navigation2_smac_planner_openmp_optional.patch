diff --git a/nav2_smac_planner/CMakeLists.txt b/nav2_smac_planner/CMakeLists.txt
index 4bbe5c55..587f9797 100644
--- a/nav2_smac_planner/CMakeLists.txt
+++ src/ros-planning/navigation2/nav2_smac_planner/CMakeLists.txt
@@ -23,13 +23,15 @@ find_package(eigen3_cmake_module REQUIRED)
 find_package(Eigen3 REQUIRED)
 find_package(angles REQUIRED)
 find_package(ompl REQUIRED)
-find_package(OpenMP REQUIRED)
+find_package(OpenMP QUIET)
 
 if(NOT CMAKE_CXX_STANDARD)
   set(CMAKE_CXX_STANDARD 17)
 endif()
 
-set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
+if(OPENMP_FOUND)
+  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
+endif()
 
 if(MSVC)
   add_compile_definitions(_USE_MATH_DEFINES)
@@ -43,7 +45,6 @@ include_directories(
   ${OpenMP_INCLUDE_DIRS}
 )
 
-find_package(OpenMP)
 if(OPENMP_FOUND)
     set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${OpenMP_C_FLAGS}")
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
@@ -85,7 +86,10 @@ add_library(${library_name} SHARED
   src/node_basic.cpp
 )
 
-target_link_libraries(${library_name} ${OMPL_LIBRARIES} ${OpenMP_LIBRARIES}  OpenMP::OpenMP_CXX)
+target_link_libraries(${library_name} ${OMPL_LIBRARIES})
+if(OPENMP_FOUND)
+  target_link_libraries(${library_name} ${OpenMP_LIBRARIES} OpenMP::OpenMP_CXX)
+endif()
 target_include_directories(${library_name} PUBLIC ${Eigen3_INCLUDE_DIRS})
 
 ament_target_dependencies(${library_name}
