diff --git a/moveit_ros/perception/CMakeLists.txt b/moveit_ros/perception/CMakeLists.txt
index c1260ba5..24ea77b4 100644
--- a/moveit_ros/perception/CMakeLists.txt
+++ src/moveit/moveit2/moveit_ros/perception/CMakeLists.txt
@@ -53,6 +53,9 @@ find_package(moveit_msgs REQUIRED)
 find_package(moveit_ros_occupancy_map_monitor REQUIRED)
 find_package(Eigen3 REQUIRED)
-find_package(OpenMP REQUIRED)
+find_package(OpenMP QUIET COMPONENTS CXX)
+if(NOT OpenMP_CXX_FOUND)
+  message(WARNING "OpenMP CXX not found; moveit_ros_perception will build without OpenMP acceleration.")
+endif()
 find_package(OpenCV)
 
 # Finds Boost Components
diff --git a/moveit_ros/perception/lazy_free_space_updater/CMakeLists.txt b/moveit_ros/perception/lazy_free_space_updater/CMakeLists.txt
index c5f24806..8e818be8 100644
--- a/moveit_ros/perception/lazy_free_space_updater/CMakeLists.txt
+++ src/moveit/moveit2/moveit_ros/perception/lazy_free_space_updater/CMakeLists.txt
@@ -2,8 +2,11 @@ set(MOVEIT_LIB_NAME moveit_lazy_free_space_updater)
 add_library(${MOVEIT_LIB_NAME} SHARED src/lazy_free_space_updater.cpp)
 set_target_properties(${MOVEIT_LIB_NAME} PROPERTIES VERSION "${${PROJECT_NAME}_VERSION}")
-set_target_properties(${MOVEIT_LIB_NAME} PROPERTIES COMPILE_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
-set_target_properties(${MOVEIT_LIB_NAME} PROPERTIES LINK_FLAGS "${OpenMP_CXX_FLAGS}")
-if(APPLE)
+set_target_properties(${MOVEIT_LIB_NAME} PROPERTIES COMPILE_FLAGS "${CMAKE_CXX_FLAGS}")
+if(OpenMP_CXX_FOUND)
+  set_property(TARGET ${MOVEIT_LIB_NAME} APPEND_STRING PROPERTY COMPILE_FLAGS " ${OpenMP_CXX_FLAGS}")
+  set_property(TARGET ${MOVEIT_LIB_NAME} APPEND_STRING PROPERTY LINK_FLAGS " ${OpenMP_CXX_FLAGS}")
+endif()
+if(TARGET OpenMP::OpenMP_CXX)
   target_link_libraries(${MOVEIT_LIB_NAME} OpenMP::OpenMP_CXX)
 endif()
 ament_target_dependencies(${MOVEIT_LIB_NAME}
