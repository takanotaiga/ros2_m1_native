--- a/ros/src/foxglove_bridge/CMakeLists.txt
+++ src/foxglove/foxglove-sdk/ros/src/foxglove_bridge/CMakeLists.txt
@@ -29,6 +29,10 @@
   else()
     target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic -Werror -Wold-style-cast -Wfloat-equal)
   endif()
+  if(APPLE)
+    # Channel IDs are uint64_t; keep strict warnings while allowing platform-specific printf width differences.
+    target_compile_options(${target} PRIVATE -Wno-error=format)
+  endif()
 endmacro()
 
 find_package(nlohmann_json QUIET)
@@ -75,6 +79,12 @@
   elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64")
     set(FOXGLOVE_SDK_PLATFORM "x86_64-unknown-linux-gnu")
     set(FOXGLOVE_SDK_SHA "8e4967ec7446a06b5a04c47b54bcd4cecff2ad8776e002ef3803ed4a7076841d")
+  elseif(CMAKE_SYSTEM_NAME STREQUAL "Darwin" AND (CMAKE_SYSTEM_PROCESSOR STREQUAL "arm64" OR CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64"))
+    set(FOXGLOVE_SDK_PLATFORM "aarch64-apple-darwin")
+    set(FOXGLOVE_SDK_SHA "0799fcb999616fe25d16d7e4b2894adb6f7fa97ce9671bd908d8412505887bec")
+  elseif(CMAKE_SYSTEM_NAME STREQUAL "Darwin" AND CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64")
+    set(FOXGLOVE_SDK_PLATFORM "x86_64-apple-darwin")
+    set(FOXGLOVE_SDK_SHA "3e0d7e575e7de9351ae71a0201ede5c00020f2e9d12cc5cd08e2c896033c9fa9")
   else()
     message(FATAL_ERROR "Unsupported platform/architecture combination: ${CMAKE_SYSTEM_PROCESSOR}-${CMAKE_SYSTEM_NAME}")
   endif()
@@ -99,6 +109,14 @@
   target_link_libraries(foxglove_cpp_static PRIVATE ${foxglove_sdk_SOURCE_DIR}/lib/libfoxglove.a)
 endif()
 
+if(APPLE)
+  # The prebuilt SDK archive references Security/CoreFoundation symbols on macOS.
+  target_link_libraries(foxglove_cpp_static INTERFACE
+    "-framework Security"
+    "-framework CoreFoundation"
+  )
+endif()
+
 find_program(GIT_SCM git DOC "Git version control")
 if (GIT_SCM)
   execute_process(
